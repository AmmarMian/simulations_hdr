# Chapter 1 · Context

Background figures illustrating core statistical concepts from the dissertation introduction.

## Scripts

| Script | Figure | Description |
|--------|--------|-------------|
| `scm_grandnombres.py` | — | SCM convergence (law of large numbers regime) |
| `scm_underregime.py` | — | SCM behaviour in the under-sampled regime |
| `lwf_underregime.py` | — | LWF estimator under the under-sampled regime |
| `cone_spd/main.py` | — | Cone of SPD matrices visualisation |
| `examples_covariances/main.py` | — | Covariance matrix example gallery |

## Running

These are standalone scripts with no qanat wiring — run directly:

```sh
uv run python 1-context/scm_grandnombres.py
```
